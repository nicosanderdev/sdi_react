-- Deprecated helper. Do not apply this file: the secured definition lives in
-- supabase/migrations (regenerate_property_ical_export_token requires is_property_owner).
-- Kept so older docs that point here do not reintroduce an unauthenticated SECURITY DEFINER.
SELECT 1;
