# Environment variables for Playwright tests

# Test user credentials (optional - defaults provided in code)
TEST_USER_EMAIL=nico1@nico.com
TEST_USER_PASSWORD=P@ssw0rd!

# Application URLs (if different from baseURL in config)
# BASE_URL=http://localhost:5173

# Supabase configuration (required for app + most e2e)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Admin reservations e2e (e2e/admin-reservations.spec.ts) — server-side only, never expose to the browser
# SUPABASE_URL=https://your-project.supabase.co
# Use the JWT service_role key (three dot-separated parts, usually starts with eyJ) from Dashboard → API.
# Do NOT use sb_secret_… keys here — PostgREST returns PGRST301 "Expected 3 parts in JWT".
# SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Admin login must match a user that has (or will get) Members + Owners + EstateProperties for seeding
# ADMIN_USER_EMAIL=admin@testcompany.com
# ADMIN_USER_PASSWORD=adminpassword123

# API configuration (if needed for mocking or direct API testing)
# API_BASE_URL=http://localhost:8080/api
# TENANT_API_KEY=your-tenant-api-key

# Authentication settings
# AUTH_TIMEOUT=30000
# SAVE_AUTH_STATE=true

# Test configuration
# CI=true  # Set to true in CI environments
